<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Main Activity (Login Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WebView Activity for Google OAuth -->
        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />

        <!-- Home Activity (Main Dashboard) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- Claims Form Activity -->
        <activity
            android:name=".ClaimsFormActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- Timetable Activity -->
        <activity
            android:name=".TLATimetableActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- About Activity -->
        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- Admin Activity -->
        <activity
            android:name=".AdminActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- Manager Activity -->
        <activity
            android:name=".ManagerActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- Reset Password Activity -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- Unauthorized Access Activity -->
        <activity
            android:name=".UnauthorizedActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- Not Found Activity -->
        <activity
            android:name=".NotFoundActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

    </application>
</manifest>